# SkillTriage 整理报告

## 整理结论

## 本次如何判断

## 本次扫描结果

## Agent 调用边界评估

快速读法：

- 高风险：
- 中风险：
- 低风险：
- 只提示不处理：

| 分组 | 混淆风险 | Agent 判断 | 建议 |
|---|---|---|---|

## 重点分组详情

## 相似项概览

## 相关功能组概览

## 需要你决定

下面这些不是高置信清理项。它们都有保留理由，取舍取决于你的使用习惯。你可以现在决定，也可以先不处理。

| 项目 | 为什么需要你决定 | 可选处理 | 是否记为以后整理时的偏好 |
|---|---|---|---|

偏好只影响提示和排序，不会自动清理 skill，也不能替代本次 Agent 评估。

## 建议下一步

## 覆盖范围与可能遗漏

### 宽泛功能组提示

## 可选整理执行

默认建议先阅读报告，不立即执行。

如果你想继续，可以让当前 Agent 进入整理执行流程。它会逐条带你审阅建议，只处理你明确批准的项，并先生成 staged 变更。执行前会检查备份、路径和 hash；执行后可以通过本次 run 的恢复材料回退。

真正写入前还会有一次确认：staged 后会生成一段确认短语。你确认后，当前 Agent 才能写入 `execution/approval.json` 并继续 apply。如果要处理的项目变了，需要重新确认。

恢复不是无条件覆盖。回退前仍会检查目标路径、文件 hash、run 目录、恢复材料和写入权限；如果发现文件后来被手动改过、目标路径已经被占用，或恢复材料缺失，SkillTriage 会停下来让你确认。

本次可执行建议：

- 可归档候选：
- 可改写 description：
- 只建议不执行：

如果本次是 `backup=off`，整理执行会被阻止；请先用 `targeted` 或 `full` backup 重新扫描。

## 没有修改的内容
