[project]
name = "memory-reconciler"
version = "0.1.0"
description = "Read-only-first CLI for Hermes memory reconciliation"
requires-python = ">=3.11"

[project.scripts]
memory-reconciler = "memory_reconciler.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
